/* generated by Svelte v3.24.0 */
import {
	SvelteComponent,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	space
} from "svelte/internal";

function create_fragment(ctx) {
	let blockquote0;
	let t1;
	let p1;
	let t3;
	let p2;
	let t5;
	let blockquote1;
	let t7;
	let p4;

	return {
		c() {
			blockquote0 = element("blockquote");
			blockquote0.innerHTML = `<p>There’s a big difference between having smart, reusable patterns at your disposal and creating a dictatorial culture designed to enforce conformity and swat down anyone coloring outside the lines.</p>`;
			t1 = space();
			p1 = element("p");
			p1.textContent = "Frost is responding to the concert that design systems can enforce a Taylorist dehumanization of our work, but that they only do this as a reflection of existing organizational principles.";
			t3 = space();
			p2 = element("p");
			p2.textContent = "Working with other leaders in the design systems world, Frost suggests that design systems should be boring – that is they should primarily focus on the parts of the process that are foundational and allow of the team to do spend time on the interesting problems.";
			t5 = space();
			blockquote1 = element("blockquote");
			blockquote1.innerHTML = `<p>Not having to reinvent things from scratch every time you need something is generally a good idea.</p>`;
			t7 = space();
			p4 = element("p");
			p4.textContent = "The work that we do under the system of ‘Agile’ is part of the organization structure that pushes for Taylorized dehumanization of web development. Our systems that we build have the ability to either support individual expression and humanity, or to suppress it. The difference between these outcomes is the broader system underlying our approaches to work.";
		},
		m(target, anchor) {
			insert(target, blockquote0, anchor);
			insert(target, t1, anchor);
			insert(target, p1, anchor);
			insert(target, t3, anchor);
			insert(target, p2, anchor);
			insert(target, t5, anchor);
			insert(target, blockquote1, anchor);
			insert(target, t7, anchor);
			insert(target, p4, anchor);
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(blockquote0);
			if (detaching) detach(t1);
			if (detaching) detach(p1);
			if (detaching) detach(t3);
			if (detaching) detach(p2);
			if (detaching) detach(t5);
			if (detaching) detach(blockquote1);
			if (detaching) detach(t7);
			if (detaching) detach(p4);
		}
	};
}

const metadata = {
	"title": "Design Systems, Agile, and Industrialization",
	"slug": "design-systems-agile-and-industrialization",
	"author": "Brad Frost",
	"year": 2020,
	"source": "https://bradfrost.com/blog/post/design-systems-agile-and-industrialization/",
	"thesis": "Design systems are reflections of organizational processes.",
	"tags": "design systems, design/development",
	"type": "annotation, bibliography"
};

const { title, slug, author, year, source, thesis, tags, type } = metadata;

class Component extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, null, create_fragment, safe_not_equal, {});
	}
}

export default Component;
export { metadata };